---
- hosts: [ 'app-staging' ]

  vars_files:
    - group_vars/securedrop.yml
    - host_vars/app.yml
    - securedrop-app-staging-conf.yml

  roles:
    - common
    - app-debs
    - app
    - app-test

  sudo: yes
