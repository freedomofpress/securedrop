---
### General (used by more than one role) ###
securedrop_code: /var/www/securedrop
securedrop_data: /var/lib/securedrop
securedrop_user: www-data

app_hostname: app-staging
monitor_hostname: mon-staging

apache_listening_address: 0.0.0.0
apache_source_log: /var/log/apache2/source-error.log
apache_allow_from: all

# Hard-coding the default interface used in the
# staging rules for now. In VirtualBox, eth0 is a
# NAT device used for SSH port forwards. In DigitalOcean,
# eth0 is the only device, so still the correct device
# to allow staging access to. TODO: virtualize firewall
# so these iface values are more flexible. Also consider
# enabling private_networking in DO.
staging_iface: eth0

# Staging machines receive deb packages, but will still need the FPF
# repo to install grsecurity (only relevant to VirtualBox).
tor_repo_info:
  repo_line: deb http://deb.torproject.org/torproject.org
  filename: deb_torproject_org_torproject_org.list
  fingerprint: A3C4F0F979CAA22CDBA8F512EE8CBC9E886DDD89
  public_key_info: >
    pub  2048R/886DDD89 2009-09-04 deb.torproject.org archive signing key
          Key fingerprint = A3C4 F0F9 79CA A22C DBA8  F512 EE8C BC9E 886D DD89
    sub  2048R/219EC810 2009-09-04 [expires: 2018-08-30]

fpf_repo_info:
  repo_line: deb [arch=amd64] https://apt.freedom.press
  filename: apt_freedom_press.list
  fingerprint: B89A29DB2128160B8E4B1B4CBADDE0C7FC9F6818
  public_key_info: >
    pub  4096R/FC9F6818 2014-10-26 Freedom of the Press Foundation Master Signing Key
          Key fingerprint = B89A 29DB 2128 160B 8E4B  1B4C BADD E0C7 FC9F 6818
    sub  4096R/4833B9A3 2014-10-26
